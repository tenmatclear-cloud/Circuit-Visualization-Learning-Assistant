# 教學文字範例

改這個檔後，重新整理網頁，左側按鈕就會用新文字。

每個範例三段：

1. 標題：`編號 | 中文按鈕 | English button`
2. `zh`：中文提示詞
3. `en`：English prompt

真正的範例標題列必須從行首寫 `## `。下面兩個已準備好，可直接改段落文字。

---

## series | 串聯示例 | Series

### zh
請設計一個簡單電路：包含一個 9V 電池、一個開關，以及兩個串聯的燈泡。請用燈泡（lamp），不要用電阻。佈局要清晰，方便學生觀察電流只有一條路徑，以及兩個燈泡的亮度。除非必要，請不要加入文字標籤。

### en
Please design a simple circuit with one 9V battery, one switch, and two lamps in series. Use lamps, not resistors. Keep the layout clear so students can observe the single current path and the brightness of both lamps. Avoid text labels unless they are truly necessary.

## parallel | 並聯示例 | Parallel

### zh
請設計一個簡單電路：包含一個 9V 電池、一個開關，以及兩個並聯的燈泡。請用燈泡（lamp），不要用電阻。佈局要清晰，方便學生觀察電流在分岔後分開，以及兩個燈泡的亮度。除非必要，請不要加入文字標籤。

### en
Please design a simple circuit with one 9V battery, one switch, and two lamps in parallel. Use lamps, not resistors. Keep the layout clear so students can observe the current splitting at the branch and the brightness of both lamps. Avoid text labels unless they are truly necessary.
