# 電路視覺化教學助手說明書

這份說明書講解網頁上的全部使用方法。適合學生、老師，以及要準備課堂的人。

網站用來把**文字需求**或**電路圖／實驗室相片**變成可互動的電路，再在右側模擬器觀察電流、電壓，以及串聯與並聯的分別。它不是計算機，也不會直接給出測驗答案。

---

## 1. 開始之前

請用瀏覽器打開：

```txt
http://localhost:8080
```

不要直接雙擊 `index.html`。這樣做時，後端 API 與右側模擬器通常不能正常運作。

本機啟動方式（擇一即可）：

1. 雙擊專案裡的 `serve.command`
2. 在專案資料夾執行 `ruby server.rb`

如果頁面頂部出現紅色或橙色提示，代表後端尚未啟動，或尚未設定 API key。請先啟動 server，再按右上角「API key」填入 Poe 金鑰。本機也可以改用 `server-config.local.json`。

線上部署（例如 Render）則直接打開該網站網址即可。

---

## 2. 畫面總覽

頁面由上到下分為四個主要部分：

1. **標題列**：網站名稱、繁中／English 切換、API key 入口、課堂展示按鈕
2. **使用步驟**：標題列正下方的可收合說明，預設打開
3. **左側「輸入需求」**：文字、圖片、生成按鈕，以及進階輸出
4. **右側「電路模擬」**：內嵌 Falstad / CircuitJS 模擬器

生成成功後，電路會自動出現在右側。一般情況不必打開左側的「電路代碼」。

---

## 3. 語言切換

右上角可在「繁中」與「English」之間切換。

- 介面文字會一起轉成中文或英文
- 右側模擬器的選單語言也會同步切換
- 左側「串聯示例／並聯示例」的提示詞會改成對應語言
- 你的選擇會記在瀏覽器裡，下次打開仍會沿用

課堂上若學生習慣英文儀器名稱，可切到 English；講解香港課程時，通常用繁中較清楚。

右上角另有「API key」。金鑰只存在這個瀏覽器，按「儲存」後即可生成電路。可到 `https://poe.com/api/keys` 建立。若伺服器已用 `server-config.local.json` 或環境變數設定，這裡可留空。「清除」只刪這個瀏覽器裡的金鑰。課堂展示時會隱藏這個入口。

---

## 4. 建議流程（四步）

1. **輸入**：在左側寫文字，或上載一張電路圖／相片。也可按「串聯示例」「並聯示例」一鍵填入。若只要現成電路、不想等生成，先打開「範例電路」。
2. **生成**：按「生成電路」，等待狀態由「生成中」變成「生成完成」。
3. **觀察**：看右側黃色小點（電流方向）和顏色深淺（電壓高低）。可點開關、拖曳元件。
4. **延伸（可選）**：再開「教學指引」或「解題教學草稿」，配合模擬器提問。

如果只要投影電路給全班看，生成後按右側的「課堂展示」。按 `Esc` 或「離開展示」即可返回。

---

## 5. 範例電路頁

左側「輸入需求」旁的「範例電路」會打開 `/examples.html`。這一頁**不呼叫 AI**，專門給老師即時示範，也給學生對比自學。按「在主頁模擬」後，電路會自動載入右側模擬器。

何時用這一頁：

- 課堂上要立刻出示串聯、並聯或綜合電路，不想等「生成電路」
- 學生想並排看多種畫法，加深「一條路／分岔／先串再並」的印象
- 要把現成 Falstad 代碼複製或載入主頁模擬器

九條都用 `diagrams/` 裡已準備的圖：

| 編號 | 圖 | 接法 |
| --- | --- | --- |
| `series-1` | 兩個燈泡串聯 | 上邊開關與電池，底邊兩燈 |
| `series-2` | 三個燈泡繞圈串聯 | 左、底、右各一燈 |
| `series-3` | 三個燈泡曲折串聯 | 看起來像分層，其實仍是一條路 |
| `parallel-1` | 兩個電阻並聯 | 上下並排，左右有接點 |
| `parallel-2` | 三個電阻並聯 | 電池在左、開關在底的梯級畫法 |
| `parallel-3` | 電源放中間的並聯 | 與兩電阻並聯相同，電源畫在中間 |
| `combine-1` | 先串聯再並聯 | 一隻電阻先串，再接兩隻並聯 |
| `combine-2` | 左邊串聯、另外兩隻並聯 | 與上一條同一類型、佈局不同 |
| `combine-3` | 五電阻橋式綜合 | 四邊加一條對角 |

圖上開關是打開的。模擬器裡也一樣；點一下合上才會看到電流。

每張卡可以：

1. **複製代碼**：把 Falstad 代碼複製到剪貼簿，再貼進主頁「電路代碼」，按「載入右側模擬器」
2. **在主頁模擬**：直接打開主頁並載入該條電路，例如 `/?example=series-1`

篩選可只看串聯、並聯或綜合。語言切換與主頁共用，會記在同一個瀏覽器設定裡。

---

## 6. 方法一：用文字生成電路

### 6.1 怎麼寫

在「文字需求」框描述你想看到的電路。愈具體愈穩定。

建議寫明：

- 電源：例如「一個 9V 電池」
- 元件：燈泡、電阻、開關、安培計、伏特計、可變電阻
- 接法：串聯、並聯，或哪一段並聯
- 觀察目的：例如「方便比較兩個燈泡的亮度」

好的例子：

```txt
請設計一個簡單電路：包含一個 9V 電池、一個開關，以及兩個串聯的燈泡。請用燈泡，不要用電阻。佈局要清晰，方便觀察電流只有一條路徑。
```

較弱的例子：

```txt
畫一個電路
```

### 6.2 內建文字示例

「生成電路」旁邊有「串聯示例」和「並聯示例」。按下去會把準備好的提示詞填入文字框，再按「生成電路」即可。

這些文字來自專案裡的 `examples.md`。老師若要改示例內容，改該檔後重新整理網頁即可。

### 6.3 清除文字

文字框有內容時，右上角會出現「清除文字」。按下去會清空文字，但不會移除已上載的圖片。

---

## 7. 方法二：用電路圖或相片生成電路

### 7.1 可接受的檔案

在「電路圖圖片（可選）」選擇檔案。接受：

- JPEG / JPG
- PNG
- WebP
- 檔案小於 8MB

上載後會看到預覽。網站會把圖片壓縮到約 1280px，再送給後端辨識。可按「移除圖片」取消。

文字和圖片可以同時使用。例如上載題目圖，再在文字框補一句「紅線由電池正極接到開關，再接到燈泡 X」。這樣通常比只丟相片更準。

### 7.2 課本電路圖

清晰的課本圖、手繪簡圖、投影片截圖都適合。請盡量：

- 讓整個迴路入鏡
- 電池、開關、燈泡、電錶的符號清楚
- 如果圖上有 X、Y、A、V、R1 等名稱，保持可讀

### 7.3 真實實驗室相片

實物相片比課本圖困難，因為導線會彎、會遮擋。建議：

1. 從正上方或斜上方拍攝，看到每個元件兩端端子
2. 光線充足，避免反光、陰影和模糊
3. 整個電路入鏡，不要切走電池、開關或錶的接線端
4. 把未使用的導線移開
5. 安培計／伏特計的 A、V 標籤盡量清楚
6. 上載時加一兩句文字，說明哪條線接到哪裡
7. 電路很複雜時，可先拍一支路，再逐步擴展
8. 最理想是相片加手繪簡圖：相片看元件，簡圖確認接法

### 7.4 範例電路圖（可下載）

可以。網站已放了一張課本式 JPEG，供學生下載或直接載入，練習「用圖片生成電路」。

- 檔案位置：`assets/examples/sample-circuit.jpg`
- 網頁上：標題下方的使用步驟裡，按「下載範例電路圖」
- 也可按「載入範例圖」，圖片會直接進入左側上載欄，再按「生成電路」即可

這張預設圖是：

- 一個 9V 電池
- 一個開關 S（打開）
- 兩個串聯燈泡 X、Y
- 只有一條電流路徑

生成後可請學生觀察：開關打開時電流是否停止；把開關合上後，兩個燈泡是否一樣亮。

#### 想換成自己的電路圖

可以。把你的 JPEG 覆蓋同一個檔即可：

```txt
assets/examples/sample-circuit.jpg
```

建議：

- 用 JPEG，或改副檔名後一併改網頁連結
- 小於 8MB
- 白底、符號清楚、元件有標籤（例如 S、X、Y）
- 不要放太多裝飾或與電路無關的文字

若要改檔名或路徑，需要同時改：

1. `index.html` 裡下載連結的 `href`
2. `app.js` 裡的 `sampleCircuitImage`

專案也附有 `assets/examples/render_sample_circuit.py`。若只想重畫預設那張串聯圖，先安裝 `pip install "schemdraw[matplotlib]"`，再執行：

```bash
python3 assets/examples/render_sample_circuit.py
```

---

## 8. 生成電路

準備好文字或圖片後，按左側的「生成電路」。

過程中會看到：

- 狀態變成「生成中」
- 出現紅色「停止生成」按鈕
- 下方提示「正在生成電路...」

完成後：

- 電路自動載入右側模擬器
- 狀態變成「生成完成」
- 提示你可以直接觀察電流與電壓

如果佈局歪了或某條線接錯，**直接在右側用滑鼠拖曳改正**，不必先改代碼。

按「停止生成」可取消目前這一輪。取消後可改提示詞再試。

---

## 9. 右側模擬器

### 9.1 看什麼

| 畫面 | 意思 |
| --- | --- |
| 黃色小點沿導線移動 | 電流方向 |
| 顏色深淺 | 電壓高低 |
| 燈泡亮度 | 該支路功率／電流大小 |
| 開關開合 | 迴路是否接通 |

如果小點太快，可在模擬器右側把「電流速度」調慢。本網站載入電路時，已把電流速度調得較適合課堂觀看。

### 9.2 常用操作

- **拖曳元件或接線端點**：改正歪掉或接錯的線
- **右鍵**：刪除接錯的線或元件
- **點擊開關**：開或合
- **拖曳可變電阻的滑動端**：改變電阻值

觀察重點：

- 串聯：電流是否只有一條路徑；兩個燈泡是否一樣亮
- 並聯：電流是否在分岔後分開；關掉一盞燈時，另一盞是否仍亮

### 9.3 模擬器未出現

右側若出現「本地 Falstad 尚未成功載入」：

1. 確認已用 `serve.command` 或 `ruby server.rb` 啟動
2. 網址是 `http://localhost:8080`，不是檔案路徑
3. 重新整理頁面
4. 仍不行時，打開右側「進階：模擬器工具」，按「重新檢查模擬器」

### 9.4 課堂展示

按「課堂展示」後：

- 左側輸入區會收起
- 使用步驟、提示卡會隱藏
- 模擬器放大，方便投影

按右上角「離開展示」，或按鍵盤 `Esc`，返回一般畫面。

---

## 10. 教學指引

「教學指引」在左側，預設收起。

先生成電路，再按「生成教學指引」。內容是觀察重點與課堂操作建議，例如該看哪條電流路徑、該開合哪個開關、該比較哪兩個燈泡。

用途：

- 老師備課時先看一遍
- 投影後按指引帶領學生觀察
- 複製後貼到工作紙或簡報

它根據**目前已生成的電路代碼**來寫，所以請先有電路再按。

---

## 11. 解題教學草稿

「解題教學草稿」也在左側，預設收起。

先生成電路，再按「生成解題教學」。內容是引導式提問與教學流程，不是直接寫出數值答案。

適合：

- 老師設計提問順序
- 學生先自己想，再對照模擬器
- 把草稿改寫成課堂講稿

同樣要先有電路才能生成。

---

## 12. 進階：電路代碼

「電路代碼」預設收起。一般學生不必打開。

這裡顯示 Falstad / CircuitJS 的匯入代碼。生成電路後會自動填入，並載入右側。

進階用途：

- **複製**：把代碼貼到其他 Falstad 視窗
- **手動改數值**：例如把電池由 9V 改成 6V，或改電阻值
- **載入右側模擬器**：改完代碼後按此鈕，才會套用到右側
- **從右側匯出目前電路**：若你已在模擬器裡拖過線路，可把目前電路寫回這個框

### 12.1 學生可安全改的地方

代碼一行一個元件，用空格分隔，座標必須是 16 的倍數。

常見改法：

1. 電池電壓：找到以 `v` 開頭的那一行，改電壓數字，例如 `9` 改成 `6`
2. 電阻值：找到以 `r` 開頭的那一行，改最後一個數字
3. 開關：找到以 `s` 開頭的那一行，`1` 是打開，`0` 是閉合
4. 移動元件：同時改兩端座標，並保持是 16 的倍數

不要刪第一行 `$ ...`，也不要隨便改燈泡、安培計、伏特計後面的固定參數。每次只改一兩個地方，再按「載入右側模擬器」。

更完整的代碼格式說明見專案 `README.md` 的「Falstad code 快速讀法」。

---

## 13. 進階：除錯輸出

「除錯輸出」預設收起。生成失敗，或電路明顯不對時才需要打開。

這裡顯示模型交給後端的內容，以及本機編譯結果。常見標記：

- `[Circuit Schema]`：模型辨識出的課程元件
- `[Compiled Falstad Code]`：本機編譯出的電路代碼
- `[Schema Fallback]`：schema 失敗後的後備說明
- `[Direct Falstad Fallback]`：直接生成代碼時的模型輸出
- `[Guide]` / `[Tutor]`：教學指引或解題草稿的原始輸出

老師可用它判斷：是相片看不清、提示詞太含糊，還是這一次生成不穩。可按「複製」把內容存起來。

---

## 14. 進階：模擬器工具

右側下方「進階：模擬器工具」預設收起，內有兩個按鈕：

- **重新檢查模擬器**：模擬器沒連上時再試一次
- **從右側匯出目前電路**：把你在模擬器裡改過的電路寫回左側代碼框，並複製到剪貼簿

在模擬器裡拖過線之後，若還想保存或再生成教學指引，先匯出一次，讓左側代碼與畫面一致。

---

## 15. 網站會辨識哪些元件

生成時，系統主要使用香港中學常見元件：

| 元件 | 說明 |
| --- | --- |
| 電池 / 電源 | 直流電源，可指定電壓 |
| 電阻 | 定值電阻 |
| 電池內阻 | 接在電源上的內電阻 |
| 可變電阻 | 滑動變阻器 |
| 燈泡 | 用真正的燈泡元件，不用電阻代替 |
| 開關 | 可開合 |
| 安培計 | 應串聯接入 |
| 伏特計 | 應並聯在要量的兩端 |
| 導線 | 所有轉角、外框、分支 |

預設不會加裝飾用的文字、箭頭或指示線。只有原圖或你的文字明確寫了 X、Y、A、V、R1、S1 等名稱時，才會加上標籤。

---

## 16. 狀態與提示分別代表什麼

左側狀態：

- **尚未生成**：還沒有成功的電路
- **生成中**：正在等候結果
- **生成完成**：這一次成功
- **生成失敗**：請看下方紅字，必要時打開「除錯輸出」

右側狀態：

- **等待載入** / **檢查中**：模擬器尚未就緒
- **已連線**：模擬器可用，可接收電路
- **已載入新電路**：剛剛匯入成功
- **未找到**：iframe 裡的模擬器沒有連上

常見提示：

- 「請先輸入文字需求，或上載一張電路圖。」：兩個都是空的
- 「請先生成電路，再進行這一步。」：教學指引或解題教學需要先有代碼
- 「只接受 PNG、JPEG 或 WebP 圖片。」：檔案格式不對
- 「圖片太大，請改用 8MB 以下的檔案。」：請縮小或改用截圖

---

## 17. 常見問題

**問：我可以直接雙擊 HTML 來用嗎？**  
不行。請用 `http://localhost:8080`。

**問：文字和圖片一定要分開用嗎？**  
不必。可以同時用：圖片負責外形，文字負責補充接線或元件名稱。

**問：生成出來的線歪了，要不要重生成？**  
通常不用。在右側拖曳端點即可。只有元件種類或接法整段錯了，才值得改提示詞再生成。

**問：為什麼燈泡看起來不像電阻？**  
這是刻意的。課程裡的燈泡會用燈泡元件，方便比較亮度。

**問：可以放一張 JPEG 給學生下載嗎？**  
可以。預設已有 `assets/examples/sample-circuit.jpg`。換成你的圖時，覆蓋同名檔即可。詳見第 7.4 節。

**問：學生會不會用這個網站直接得到數值答案？**  
網站的設計是視覺化與引導提問，不是代做計算題。解題教學草稿也以提問為主。

**問：英文介面下，說明書也是英文嗎？**  
網頁標題下的簡易步驟會隨語言切換。這份完整說明書以繁體中文書寫。

---

## 18. 給老師的課堂建議

1. 第一節先打開「範例電路」頁，用現成圖對比串聯、並聯與綜合，再按「在主頁模擬」看電流。
2. 若要即場改電路，再用主頁的「串聯示例」「並聯示例」生成，或下載／載入範例 JPEG，示範「題目圖 → 模擬電路」。
3. 請學生帶自己的課本圖或實驗相片來試；失敗時一起改拍攝角度或補一句文字。
4. 生成後立刻進入「課堂展示」，避免投影時左側框太多。
5. 先觀察，再打開「教學指引」。不要一開始就打開電路代碼。
6. 若要改電壓或電阻，進階學生可改代碼後「載入右側模擬器」；其餘學生用模擬器拖曳即可。

---

## 19. 相關檔案

| 檔案 | 用途 |
| --- | --- |
| `說明書.md` | 本文件，完整使用說明。網頁上的「完整說明書」會打開 `/manual.md` |
| `examples.html` | 範例電路頁，九條現成圖與 Falstad 代碼 |
| `examples.md` | 左側「串聯／並聯示例」的提示詞 |
| `diagrams/` | 範例頁使用的九張電路 JPEG |
| `assets/examples/library/` | 範例頁目錄與 Falstad 代碼 |
| `assets/examples/sample-circuit.jpg` | 供下載與載入的範例電路圖 |
| `index.html` | 主頁畫面 |
| `README.md` | 開發、啟動、部署與代碼格式 |

回到網頁後，標題下方的使用步驟可隨時收起，需要時再打開。
